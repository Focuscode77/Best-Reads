<button class="btn btn-primary" data-toggle="modal" data-target=".bs-example-modal-sm2">Big modal</button>

<div class="modal fade bs-example-modal-sm2">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <h4 class="modal-title">Edit field</h4>
                <button type="button" class="close" data-dismiss="modal" aria-hidden="true">&times;</button>
            </div>
            <div class="modal-body">
                <p>Current: \{{current}}</p>
                <p>New: <input class="form-control form-control-lg ml-3 w-50" type="text" placeholder="enter changes"
                        aria-label="Change"></p>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-default" data-dismiss="modal">Close</button>
                <button type="button" class="btn btn-primary">Save changes</button>
            </div>
        </div><!-- /.modal-content -->
    </div><!-- /.modal-dialog -->
</div><!-- /.modal -->


</div>

<div id="profile"></div>
<script id="profile-template" type="text/x-handlebars-template">
<img src="\{{image}}">
<h1>\{{username}}'s Profile</h1>
<p>Name: \{{name}}</p>
<p>email: \{{email}}</p>
<p>Date of birth: \{{dob}}</p>
<ul>
<li>[PH]Past</li>
<li>[PH]Current</li>
<li>[PH]Future</li>
<li>[Note: UL LI tages are also [PH]]</li>
</ul>
   



</script>
<script type="text/javascript">
    var template = $("#profile-template").html();
    console.log(template);
    var compiledProfile = Handlebars.compile(template);
    console.log(compiledProfile(profile));
    $.get("/user").then(data => {
        var dataSet = {
            image: data.pictureURL,
            username: data.username,
            name: data.name,
            email: data.email,
            dob: data.dob
        }
        $("body").append(compiledProfile(dataSet))
    })
    $('#elem').popover()

</script>

